/target/
.tools/
**/*.rs.bk
.DS_Store

# Generated by scripts/render-dev-tap-formula.sh from the live dogfood release; regenerate on
# demand rather than committing a formula that goes stale the moment a new build lands.
/packaging/homebrew/*.rb

# Agent Relay's per-project runtime state is intentionally local.
.relay/

.claude/settings.local.json
